services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: Tusk
      POSTGRES_PASSWORD: Tusk3110
      POSTGRES_DB: demo_arquitectura
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: